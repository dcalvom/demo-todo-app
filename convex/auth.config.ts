// This project uses custom JWT authentication via Node.js actions.
// The auth.config.ts is not used for external providers in this setup.
// JWT verification is handled manually via jose in the auth action.
export default {
  providers: [],
};
